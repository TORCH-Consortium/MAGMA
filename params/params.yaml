# Replace these values to reflect your samplesheet and file-paths
#
# Samplesheets

input_samplesheet: "/home/magma-runs/data/EarlyMGIT.10samples.slurm.csv"

# Main output dir

outdir :  "/home/magma-results-earlymgit10samples"

# Location for manually created conda-envs

conda_envs_location :  "/home/magma_conda_envs"

# Some boolean options
optimize_variant_recalibration :  false
compute_minor_variants :  true
dataset_is_not_contaminated :  true
only_validate_fastqs :  false
